# 为训练循环增加附加功能

本章使用了一个相对简单的训练函数，以保持代码可读性并将第五章内容控制在页面限制内。可选地，我们可以添加线性预热（linear warm-up）、余弦衰减（cosine decay）调度和梯度裁剪（gradient clipping），以提高训练的稳定性和收敛性。

你可以在 [附录 D: 为训练循环增加附加功能](../../appendix-D/01_main-chapter-code/appendix-D.ipynb) 中找到该更高级训练函数的代码。
